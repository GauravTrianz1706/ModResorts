<?xml version="1.0" encoding="UTF-8"?>
<server description="ModResorts Liberty Server">

    <!-- Enable features required by the application -->
    <featureManager>
        <feature>servlet-3.1</feature>
        <feature>jsp-2.3</feature>
        <feature>jndi-1.0</feature>
        <feature>localConnector-1.0</feature>
        <feature>monitor-1.0</feature>
    </featureManager>

    <!-- HTTP/HTTPS endpoints -->
    <httpEndpoint id="defaultHttpEndpoint"
                  host="*"
                  httpPort="9080"
                  httpsPort="9443" />

    <!-- Application deployment -->
    <webApplication id="modresorts"
                    location="modresorts.war"
                    contextRoot="/resorts"
                    name="modresorts">
    </webApplication>

    <!-- Logging configuration -->
    <logging consoleLogLevel="INFO"
             traceSpecification="*=info"
             maxFileSize="20"
             maxFiles="5" />

    <!-- Session configuration -->
    <httpSession invalidationTimeout="30m" />

    <!-- Graceful shutdown timeout -->
    <executor maxThreads="50" coreThreads="10" />

</server>
