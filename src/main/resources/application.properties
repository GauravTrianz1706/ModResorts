# Spring Boot Actuator Health Endpoint Configuration
# Expose health endpoint for container orchestration monitoring
management.endpoints.web.exposure.include=health

# Show detailed health status for all components (S3, Secrets Manager, database, disk space)
management.endpoint.health.show-details=always

# Enable liveness and readiness probes for Kubernetes/ECS
management.endpoint.health.probes.enabled=true
